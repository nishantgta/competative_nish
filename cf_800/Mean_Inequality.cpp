#include <iostream>
using namespace std ;
void solve()
{
    int n,i,j,k; cin >> n ;
    int a[n];
    for(i=0;i<n;i++) cin >> a[i];
    for(i=0;i<n;i++){
        for(j=i+1;j<n;j++){
            for(k=j+1;k<n;k++){
                
            }
        }
    }
}
int main()
{
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    int t ;
    cin >> t ;
    while(t--){
        solve();
    }
    return 0;
}